permalink: /blog/:year/:month/:title
exclude:
  - Gemfile
  - Gemfile.lock
  - TODO.md
  - README.md
sass:
  style: :nested

defaults:
  -
    scope:
      path: "v2013"
    values:
      version: "v2013"
      layout: "v2013/layout"